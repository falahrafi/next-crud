export const metadata = {
    title: "Create Customer | TailAdmin",
    description: "Create a new customer record in TailAdmin Dashboard",
  };
  
  export default function CreateLayout({
    children,
  }: {
    children: React.ReactNode;
  }) {
    return <>{children}</>;
  }